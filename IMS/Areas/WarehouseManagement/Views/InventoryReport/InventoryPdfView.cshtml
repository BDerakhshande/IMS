@using IMS.Models.ProMan
@model InventoryReportPdfViewModel

@functions {
    public string CleanString(string input)
    {
        if (string.IsNullOrEmpty(input))
            return input;
        return input.Replace(";#", ", ").Replace("#", "").Replace("&", "").Trim().TrimEnd(',');
    }

    public string ToPersianDate(DateTime date)
    {
        var pc = new System.Globalization.PersianCalendar();
        return $"{pc.GetYear(date)}/{pc.GetMonth(date):00}/{pc.GetDayOfMonth(date):00}";
    }
}

@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="fa" dir="rtl" style="height: 100%">
<head>
    <meta charset="utf-8" />
    <title>گزارش موجودی انبار</title>
    <style>
        /* Reset and Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'B Nazanin', 'Iranian Sans', Tahoma, Arial, sans-serif;
        }

        @@page {
            size: A4 portrait;
            margin: 10mm;
        }

        body {
            padding: 20px;
            color: #333;
            line-height: 1.6;
            font-size: 14px;
        }

        /* Header Styles */
        .header {
            text-align: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 2px solid #1a4a5a;
        }

            .header h1 {
                color: #1a4a5a;
                font-size: 24px;
                margin-bottom: 10px;
            }

        .report-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 15px;
            font-size: 12px;
            color: #7f8c8d;
        }

        .filters-inline {
            direction: rtl;
        }

            .filters-inline .tag {
                display: inline-block;
                background-color: #e8f4fc;
                padding: 5px 10px;
                margin: 3px;
                border-radius: 3px;
                font-size: 13px;
                white-space: nowrap;
            }


        /* Filters Section */
        .filters {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
            border-right: 4px solid #1a4a5a;
        }

            .filters h4 {
                color: #1a4a5a;
                margin-bottom: 10px;
                font-size: 16px;
            }

            .filters ul {
                list-style: none;
                padding: 0;
                margin: 0;
                display: flex;
                flex-wrap: wrap;
                gap: 10px;
                flex-direction: row-reverse ;
            }

            .filters li {
                background-color: #e8f4fc;
                padding: 5px 10px;
                display: inline-flex;
                white-space: nowrap;
                border-radius: 3px;
                font-size: 13px;
            }

        /* Table Styles */
        .data-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
            font-size: 13px;
        }

            .data-table th {
                background-color: #1a4a5a;
                color: white;
                padding: 12px 8px;
                text-align: center;
                font-weight: bold;
            }

            .data-table td {
                padding: 10px 8px;
                border-bottom: 1px solid #ddd;
                text-align: center;
            }

            .data-table tr:nth-child(even) {
                background-color: #f8f9fa;
            }

            /* مخفی کردن ردیف بدون UniqueCodes وقتی فیلتر اعمال شده */
            .data-table tr.no-unique-codes {
                display: none;
            }

        .summary-row {
            font-weight: bold;
            background-color: #d6ebf2 !important;
        }

            .summary-row td {
                border-top: 2px solid #1a4a5a;
                border-bottom: 2px solid #1a4a5a;
                padding: 12px 8px;
            }
    </style>
</head>
<body style="height: 100%">
    <div class="header">
        <h1>گزارش موجودی انبار</h1>
        <div class="report-info">
            <span>تاریخ استخراج گزارش: @ToPersianDate(DateTime.Now)</span>
            <span>تعداد رکوردها: @(Model.Items?.Count ?? 0)</span>
        </div>
    </div>

    <section class="filters">
        <h4>فیلترهای اعمال شده:</h4>
        <div class="filters-inline">
            @if (Model.Filter.Warehouses != null && Model.Filter.Warehouses.Any())
            {
                <span class="tag">
                    انبارها: @string.Join(", ", Model.Filter.Warehouses
                                    .Where(w => w.WarehouseId > 0)
                                    .Select(w => Model.WarehouseNames != null && Model.WarehouseNames.ContainsKey(w.WarehouseId)
                                    ? CleanString(Model.WarehouseNames[w.WarehouseId])
                                    : $"انبار {w.WarehouseId}"))
            </span>
                        }

            @if (Model.Filter.CategoryId.HasValue)
            {
                <span class="tag">
                    دسته‌بندی: @(Model.CategoryNames != null && Model.CategoryNames.ContainsKey(Model.Filter.CategoryId.Value)
                                    ? CleanString(Model.CategoryNames[Model.Filter.CategoryId.Value])
                                    : Model.Filter.CategoryId.Value.ToString())
            </span>
                        }

            @if (Model.ZoneNames != null && Model.ZoneNames.Any())
            {
                <span class="tag">قسمت‌ها: @string.Join(", ", Model.ZoneNames.Values.Select(z => CleanString(z)))</span>
            }

            @if (Model.SectionNames != null && Model.SectionNames.Any())
            {
                <span class="tag">بخش‌ها: @string.Join(", ", Model.SectionNames.Values.Select(s => CleanString(s)))</span>
            }

            @if (Model.Filter.GroupId.HasValue)
            {
                <span class="tag">
                    گروه: @(Model.GroupNames != null && Model.GroupNames.ContainsKey(Model.Filter.GroupId.Value)
                                    ? CleanString(Model.GroupNames[Model.Filter.GroupId.Value])
                                    : Model.Filter.GroupId.Value.ToString())
            </span>
                        }

            @if (Model.Filter.StatusId.HasValue)
            {
                <span class="tag">
                    وضعیت: @(Model.StatusNames != null && Model.StatusNames.ContainsKey(Model.Filter.StatusId.Value)
                                    ? CleanString(Model.StatusNames[Model.Filter.StatusId.Value])
                                    : Model.Filter.StatusId.Value.ToString())
            </span>
                        }

            @if (Model.Filter.ProductId.HasValue)
            {
                <span class="tag">
                    کالا: @(Model.ProductNames != null && Model.ProductNames.ContainsKey(Model.Filter.ProductId.Value)
                                    ? CleanString(Model.ProductNames[Model.Filter.ProductId.Value])
                                    : Model.Filter.ProductId.Value.ToString())
            </span>
                        }

            @if (!string.IsNullOrWhiteSpace(Model.Filter.ProductSearch))
            {
                <span class="tag">جستجوی محصول: @Model.Filter.ProductSearch</span>
            }

            @if (Model.UniqueCodesFilter != null && Model.UniqueCodesFilter.Any())
            {
                <span class="tag">کدهای یکتا: @string.Join(", ", Model.UniqueCodesFilter.Select(c => CleanString(c)))</span>
            }
        </div>
    </section>


    <table class="data-table">
        <thead>
            <tr>
                <th>نام انبار</th>
                <th>قسمت</th>
                <th>بخش</th>
                <th>دسته‌بندی</th>
                <th>گروه</th>
                <th>طبقه</th>
                <th>کالا</th>
                <th>موجودی</th>
                <th>کدهای یکتا</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Items != null && Model.Items.Any())
            {
                bool hasUniqueFilter = Model.UniqueCodesFilter != null && Model.UniqueCodesFilter.Any();

                foreach (var item in Model.Items)
                {
                    bool hasUniqueCodes = item.UniqueCodes != null && item.UniqueCodes.Any();
                    bool showRow = !hasUniqueFilter || hasUniqueCodes;

                    if (showRow)
                    {
                        <tr class="@(hasUniqueCodes ? "" : "no-unique-codes")">
                            <td>@CleanString(item.WarehouseName ?? "-")</td>
                            <td>@CleanString(item.ZoneName ?? "-")</td>
                            <td>@CleanString(item.SectionName ?? "-")</td>
                            <td>@CleanString(item.CategoryName ?? "-")</td>
                            <td>@CleanString(item.GroupName ?? "-")</td>
                            <td>@CleanString(item.StatusName ?? "-")</td>
                            <td>@CleanString(item.ProductName ?? "-")</td>
                            <td>@item.Quantity.ToString("N0")</td>
                            <td>@(hasUniqueCodes ? string.Join(", ", item.UniqueCodes.Select(c => CleanString(c))) : "-")</td>
                        </tr>
                    }
                }

                <tr class="summary-row">
                    <td colspan="7">جمع کل موجودی</td>
                    <td>@(Model.Items.Where(i => i.UniqueCodes != null && i.UniqueCodes.Any()).Sum(i => i.Quantity).ToString("N0"))</td>
                    <td></td>
                </tr>
            }
            else
            {
                <tr>
                    <td colspan="9" style="text-align:center; padding:20px;">داده‌ای برای نمایش وجود ندارد</td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
