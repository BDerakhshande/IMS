@model IMS.Application.WarehouseManagement.DTOs.ProductItemDto
@{
    ViewData["Title"] = "افزودن آیتم محصول";
    Layout = "~/Areas/WarehouseManagement/Views/Shared/_MyLayout.cshtml";
}
<link href="~/lib/bootstrap-5.1.3-dist/css/bootstrap.min.css" rel="stylesheet" />
<link href="~/lib/select2-bootstrap-5-theme-1.3.0/select2-bootstrap-5-theme.min.css" rel="stylesheet" />

<link href="~/lib/select2/css/select2.min.css" rel="stylesheet" />

<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />

<div class="container-fluid py-3" dir="rtl">

    @if (!ViewData.ModelState.IsValid)
    {
        <div class="alert alert-danger alert-dismissible fade show mb-3" role="alert">
            <h6 class="alert-heading"><i class="fas fa-exclamation-triangle me-2"></i>خطا در اعتبارسنجی</h6>
            <ul class="mb-0">
                @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                {
                    <li>@error.ErrorMessage</li>
                }
            </ul>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="row">
        <div class="col-12">
            <div class="card border-0 shadow" style="box-shadow: 0 4px 8px rgba(13, 110, 253, 0.1);">
                <div class="card-header py-2 d-flex justify-content-between align-items-center"
                     style="background-color: #1a4959; color: white;">
                    <h5 class="mb-0 text-white">
                        <i class="fas fa-plus-circle me-2"></i> @ViewData["Title"]
                    </h5>
                    <a asp-action="Index" asp-route-productId="@Model.ProductId" class="btn btn-light btn-sm">
                        <i class="fas fa-arrow-right me-1"></i> بازگشت به لیست
                    </a>
                </div>

                <div class="card-body p-3">
                    <form asp-action="Create" method="post" class="needs-validation" novalidate>
                        <input type="hidden" asp-for="ProductId" />

                        <div class="row g-3 mb-3">
                            <!-- کد یکتا (Sequence) -->
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label asp-for="Sequence" class="form-label fw-bold mb-1 small">
                                        <i class="fas fa-hashtag me-1"></i> کد یکتا
                                    </label>
                                    <div class="input-group input-group-sm">
                                        <span class="input-group-text bg-light"><i class="fas fa-barcode small"></i></span>
                                        <input asp-for="Sequence" type="number" class="form-control form-control-sm"
                                               placeholder="کد یکتا به صورت خودکار تعیین می‌شود"
                                               readonly />
                                    </div>
                                    <small class="form-text text-muted small mt-1 d-block">
                                        <i class="fas fa-info-circle me-1"></i> شماره محصول به صورت خودکار تعیین شده است.
                                    </small>
                                </div>
                            </div>

                            <!-- پروژه -->
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label asp-for="ProjectId" class="form-label fw-bold mb-1 small">
                                        <i class="fas fa-project-diagram me-1"></i> پروژه <span class="text-danger">*</span>
                                    </label>
                                    <select asp-for="ProjectId" class="form-select form-select-sm select2"
                                            asp-items="ViewBag.Projects">
                                        <option value="">-- انتخاب پروژه --</option>
                                    </select>
                                    <span asp-validation-for="ProjectId" class="text-danger small mt-1 d-block"></span>
                                </div>
                            </div>

                            <!-- وضعیت -->
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label asp-for="Status" class="form-label fw-bold mb-1 small">
                                        <i class="fas fa-info-circle me-1"></i> وضعیت <span class="text-danger">*</span>
                                    </label>
                                    <!-- وضعیت -->
                                    <select asp-for="Status" class="form-select form-select-sm select2"
                                            asp-items="Html.GetEnumSelectList<IMS.Domain.WarehouseManagement.Enums.ProductItemStatus>()" required>
                                        <option value="">-- انتخاب وضعیت --</option>
                                    </select>
                                    <span asp-validation-for="Status" class="text-danger small mt-1 d-block"></span>
                                </div>
                            </div>
                        </div>

                        <div class="border-top pt-3 mt-3 d-flex justify-content-end gap-2">
                            <button type="submit" class="btn btn-primary btn-sm px-4">
                                <i class="fas fa-save me-1"></i> ثبت آیتم
                            </button>
                            <a asp-action="Index" asp-route-productId="@Model.ProductId" class="btn btn-outline-secondary btn-sm">
                                <i class="fas fa-times me-1"></i> انصراف
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
    <script src="~/lib/select2/js/select2.min.js"></script>
    <script src="~/lib/bootstrap-5.1.3-dist/js/bootstrap.bundle.min.js"></script>

    <script src="~/js/MenuJs.js"></script>
    <script>
        $(document).ready(function () {
            // فعال سازی select2 برای همه select هایی که کلاس select2 دارند
            $('.select2').select2({
                theme: 'bootstrap-5',   // استفاده از تم bootstrap
                width: '100%',          // تطبیق با عرض فرم
                placeholder: 'انتخاب کنید',
                allowClear: true        // امکان پاک کردن انتخاب
            });
        });
        // فعال سازی اعتبارسنجی فرم
        (function () {
            'use strict'
            var forms = document.querySelectorAll('.needs-validation')

            Array.prototype.slice.call(forms)
                .forEach(function (form) {
                    form.addEventListener('submit', function (event) {
                        if (!form.checkValidity()) {
                            event.preventDefault()
                            event.stopPropagation()
                        }
                        form.classList.add('was-validated')
                    }, false)
                })
        })();

        // اعتبارسنجی real-time
        document.addEventListener('DOMContentLoaded', function () {
            const inputs = document.querySelectorAll('input, select, textarea');

            inputs.forEach(input => {
                input.addEventListener('blur', function () {
                    this.checkValidity();
                });

                input.addEventListener('input', function () {
                    if (this.classList.contains('is-invalid')) {
                        this.classList.remove('is-invalid');
                    }
                });
            });

           
            const sequenceInput = document.getElementById('Sequence');
            if (sequenceInput && !sequenceInput.value) {
             
            }
        });

       
        function generateAutoSequence() {
           
            return Math.floor(Date.now() / 1000).toString().slice(-6);
        }
    </script>
}