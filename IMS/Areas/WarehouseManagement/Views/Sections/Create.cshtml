@model IMS.Application.WarehouseManagement.DTOs.StorageSectionDto
@{
    ViewData["Title"] = "بخش جدید";
    Layout = "~/Areas/WarehouseManagement/Views/Shared/_MyLayout.cshtml";
}

<div>
    

    <div class="container-fluid py-3" dir="rtl">
        <div class="row">
            <div class="col-12">
                <div class="card border-0 shadow" style="box-shadow: var(--card-shadow); background-color: white;">
                    <div class="card-header py-2 d-flex justify-content-between align-items-center"
                         style="background-color: #1a4959; color: white;">
                        <h5 class="mb-0 text-white">
                            <i class="fas fa-layer-group me-2"></i> @ViewData["Title"]
                        </h5>
                        <a asp-action="Index" asp-route-id="@Model.ZoneId" class="btn btn-light btn-sm">
                            <i class="fas fa-arrow-right me-1"></i> بازگشت به لیست
                        </a>
                    </div>

                    <div class="card-body" style="background-color: white;">
                        <form asp-action="Create" method="post" class="needs-validation" novalidate>
                            <input type="hidden" asp-for="ZoneId" />

                            <div class="row g-2 mb-3">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label asp-for="SectionCode" class="form-label fw-bold mb-1 small">
                                            <i class="fas fa-barcode me-1 text-muted"></i> کد بخش
                                        </label>
                                        <div class="input-group input-group-sm">
                                            <span class="input-group-text bg-light"><i class="fas fa-hashtag text-primary"></i></span>
                                            <input asp-for="SectionCode" class="form-control" placeholder="کد منحصر به فرد بخش را وارد کنید" />
                                        </div>
                                        <span asp-validation-for="SectionCode" class="text-danger small mt-1 d-block"></span>
                                    </div>
                                </div>

                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label asp-for="Name" class="form-label fw-bold mb-1 small">
                                            <i class="fas fa-tag me-1 text-muted"></i> نام بخش
                                        </label>
                                        <div class="input-group input-group-sm">
                                            <span class="input-group-text bg-light"><i class="fas fa-layer-group text-primary"></i></span>
                                            <input asp-for="Name" class="form-control" placeholder="نام کامل بخش را وارد کنید" />
                                        </div>
                                        <span asp-validation-for="Name" class="text-danger small mt-1 d-block"></span>
                                        <div class="form-text small text-muted">مثال: بخش A1، قفسه‌های مرکزی</div>
                                    </div>
                                </div>
                            </div>

                            <div class="row g-2 mb-3">
                             
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label asp-for="Dimensions" class="form-label fw-bold mb-1 small">
                                            <i class="fas fa-ruler-combined me-1 text-muted"></i> ابعاد
                                        </label>
                                        <div class="input-group input-group-sm">
                                            <span class="input-group-text bg-light"><i class="fas fa-arrows-alt text-primary"></i></span>
                                            <input asp-for="Dimensions" class="form-control" placeholder="ابعاد بخش را وارد کنید" />
                                        </div>
                                        <span asp-validation-for="Dimensions" class="text-danger small mt-1 d-block"></span>
                                        <div class="form-text small text-muted">مثال: 2x3x4 متر</div>
                                    </div>
                                </div>
                            </div>

                            <div class="form-footer border-top pt-3 mt-3 d-flex justify-content-end gap-2" style="border-top-color: #dee2e6 !important;">
                                <button type="submit" class="btn btn-primary px-3" style="background-color: var(--accent-color); border-color: var(--accent-color); font-size: 0.875rem;">
                                    <i class="fas fa-save me-2"></i> ثبت بخش
                                </button>
                                <a asp-action="Index" asp-route-id="@Model.ZoneId" class="btn btn-outline-secondary" style="color: #6c757d; border-color: #6c757d; font-size: 0.875rem;">
                                    <i class="fas fa-times me-2"></i> انصراف
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="~/js/MenuJs.js"></script>
    <script>
        // فعال سازی اعتبارسنجی فرم
        (function () {
            'use strict'

            var forms = document.querySelectorAll('.needs-validation')

            Array.prototype.slice.call(forms)
                .forEach(function (form) {
                    form.addEventListener('submit', function (event) {
                        if (!form.checkValidity()) {
                            event.preventDefault()
                            event.stopPropagation()
                        }

                        form.classList.add('was-validated')
                    }, false)
                })
        })()
    </script>
}