@model List<IMS.Areas.AccountManagement.Models.Tafzil>

@{
    ViewData["Title"] = "تفصیل 1";
    Layout = "~/Areas/AccountManagement/Views/Shared/_Layout.cshtml";
    var moeinName = ViewBag.MoeinName as string;
}

<div class="container-fluid py-2" dir="rtl">
    <div class="row">
        <div class="col-12">
            <div class="card border-0 shadow-sm">
                <div class="card-header py-1 d-flex justify-content-between align-items-center" style="background-color: #1a4959; color: white;">
                    <div class="d-flex align-items-center">
                        <i class="fas fa-layer-group me-1" style="font-size: 0.85rem; margin-left: 0.5rem;"></i>
                        <h5 class="mb-0 fw-bold" style="font-size: 0.9rem;"> @ViewData["Title"] </h5>
                    </div>

                    <a href="@Url.Action("CreateTafzil1", "ChartOfAccounts", new { accountId = Model.FirstOrDefault()?.Moein?.AccountId })"
                       class="btn btn-sm btn-light d-flex align-items-center py-1 px-2"
                       style="height: 28px; font-size: 0.8rem;">
                        <i class="fas fa-plus me-1" style="font-size: 0.8rem;"></i>
                        <span>افزودن تفصیل</span>
                    </a>
                </div>

                @if (!string.IsNullOrEmpty(moeinName))
                {
                    <div class="alert alert-info alert-dismissible fade show mx-2 my-1 py-1" role="alert" style="font-size: 0.8rem;">
                        <i class="fas fa-info-circle me-1"></i>
                        @moeinName
                        <button type="button" class="btn-close p-1" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                }

                <div class="card-body p-1">
                    <div class="table-responsive">
                        <table class="table table-hover table-sm mb-0">
                            <thead style="background-color: #f8f9fa; font-size: 0.82rem;">
                                <tr>
                                    <th class="text-center py-1">کد کل</th>
                                    <th class="text-center py-1">کد معین</th>
                                    <th class="text-center py-1">کد تفصیل</th>
                                    <th class="text-center py-1">نام تفصیل</th>
                                    <th class="text-center py-1">عملیات</th>
                                </tr>
                            </thead>
                            <tbody style="font-size: 0.8rem;">
                                @foreach (var item in Model)
                                {
                                    <tr data-url="@Url.Action("SecondTafzils", "ChartOfAccounts", new { id = item.Id })" style="cursor: pointer; height: 32px;">
                                        <td class="text-center py-1 align-middle">@item.Moein?.Account?.Code</td>
                                        <td class="text-center py-1 align-middle">@item.Moein?.Code</td>
                                        <td class="text-center py-1 align-middle">@item.Code</td>
                                        <td class="text-center py-1 align-middle">@item.Name</td>
                                        <td class="text-center py-1 align-middle">
                                            <div class="d-flex justify-content-center gap-1">
                                                <a href="@Url.Action("EditTafzil1", "ChartOfAccounts", new { id = item.Id })"
                                                   class="btn btn-sm btn-outline-warning px-1"
                                                   style="font-size: 0.75rem; min-width: 26px; height: 26px;"
                                                   onclick="event.stopPropagation();">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                <form method="post" action="@Url.Action("Delete", "ChartOfAccounts")" class="d-inline">
                                                    @Html.AntiForgeryToken()
                                                    <input type="hidden" name="id" value="@item.Id" />
                                                    <button type="submit"
                                                            class="btn btn-sm btn-outline-danger px-1"
                                                            style="font-size: 0.75rem; min-width: 26px; height: 26px;"
                                                            onclick="event.stopPropagation(); return confirm('آیا از حذف این حساب مطمئن هستید؟');">
                                                        <i class="fas fa-trash-alt"></i>
                                                    </button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="d-flex justify-content-center mt-2">
                        <nav>
                            <ul class="pagination pagination-sm mb-0" id="paginationContainer"></ul>
                        </nav>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/MenuJs.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            document.querySelectorAll('tbody tr[data-url]').forEach(function (row) {
                row.addEventListener('click', function (e) {
                    if (!e.target.closest('a, button, input, .btn')) {
                        const url = row.dataset.url;
                        if (url) {
                            window.location.href = url;
                        }
                    }
                });
            });

            const rowsPerPage = 10;
            let currentPage = 1;
            const table = document.querySelector('table');

            function paginateTable() {
                const rows = Array.from(table.querySelectorAll('tbody tr'));
                const totalPages = Math.ceil(rows.length / rowsPerPage);

                rows.forEach(row => row.style.display = 'none');
                const start = (currentPage - 1) * rowsPerPage;
                const end = start + rowsPerPage;
                rows.slice(start, end).forEach(row => row.style.display = '');

                const paginationContainer = document.getElementById('paginationContainer');
                paginationContainer.innerHTML = '';

                for (let i = 1; i <= totalPages; i++) {
                    const li = document.createElement('li');
                    li.classList.add('page-item');
                    if (i === currentPage) li.classList.add('active');

                    const a = document.createElement('a');
                    a.classList.add('page-link');
                    a.href = '#';
                    a.style.fontSize = '0.75rem';
                    a.innerText = i;

                    a.addEventListener('click', function (e) {
                        e.preventDefault();
                        currentPage = i;
                        paginateTable();
                    });

                    li.appendChild(a);
                    paginationContainer.appendChild(li);
                }
            }

            paginateTable();
        });
    </script>
}
